{
  "name": "moana-live-action",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && python3 scripts/fix-routes.py",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^11.0.0",
    "sharp": "^0.34.5",
    "wrangler": "^3.80.0"
  }
}
